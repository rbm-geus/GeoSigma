# Claude Code
CLAUDE.md

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Packaging build artifacts. Regenerated by any build or `pip install -e .`,
# so they must not be tracked — a tracked egg-info goes stale (it kept
# advertising numpy+matplotlib as the only dependencies) and dirties the
# working tree on every install.
*.egg-info/
build/
dist/
grid_conversion_DKM345.py
TRANSLATION_PLAN.md

# Working notes for a forthcoming publication; kept local.
DANISH_CHOICES.md


# Private validation data — not for public release
examples/data/var_map_demo/*

# Private MATLAB-parity validation harness — loads/derives from the private
# Danish workspaces, so it stays LOCAL and is never committed. Only the eventual
# anonymised public themes demo (see TRANSLATION_PLAN.md "Final step") ships.
examples/var_map_demo_adapter.py
examples/demo_validate_themes_vs_matlab.py
examples/diag_combine_tail.py
examples/extract_combine_crop.py
examples/output/
tools/inventory_plotting_mat.py